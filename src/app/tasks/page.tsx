"use client";
import { useState } from "react";

const TasksPage = () => {
    const [tasks, setTasks] = useState<any>([]);

    return (
        <div>
            <h1>Tasks</h1>

        </div>
    );
};

export default TasksPage;